let person = {
    name: "Marcos",
    country: "China",
}
if(person.country === 'Brasil'){
    console.log(`${person.name} é brasileiro(a)!`)
}else{
    console.log(`${person.name} é estrangeiro(a)!`)
}